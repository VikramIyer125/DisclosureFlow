# Vendored copy of the canonical repo-root shared/ backbone. Created at build
# time by `make pack AGENT=scoping-agent` (rsync). It is a BUILD ARTIFACT, not
# source — the canonical source lives at <repo>/shared/. Never commit it.
shared/
